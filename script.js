// Create an array called `colors` that contains three colors.


// Access the last item in the array and assign to a variable called `last`.


// Create an empty array called `favoriteColors`.


// Create a `for` loop that pushes each item in `colors` into `favoriteColors`.


// Create an `instructor` object that contains three key-value pairs.


// Add a `facial-hair` property to `instructor` by accessing it (do not change the original object you typed above).
